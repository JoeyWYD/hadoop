注意：
-以下操作题，请将“${yourname}”替换成自己的名字。
-请不要修改或删除任何配置文件、停止或重启进程。
-dns_log是网站访问日志表，第一个字段为访问ip，第二个字段为访问域名，第三个字段为访问时间等等。
1、在hdfs中的“/0220”目录中创建一个子目录“${yourname}“。
2、将“/opt”目录下的“dns_log”文件上传到刚刚创建的目录中。
3、在hive中的“ustcinfo”的数据库中，创建一个外部表，表名称为“dns_${yourname}”，使用数据为刚刚上传的文件。
4、在hive统计访问量最高的前10名的域名以及访问次数。


